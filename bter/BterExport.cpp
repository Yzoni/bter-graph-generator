#include "BterExport.h"

void BterExport::createMap(BterEdges *bterEdges, std::unordered_map<int, vector<int>> *m) {

//    int i;
//    for (i = 0; i < bterEdges.bter_phase_one_size; ++i) {
//        // Test if key exists
//        std::unordered_map<std::string, double>::const_iterator it_i = m.find(bterEdges.phase_one_i[i]);
//        std::unordered_map<std::string, double>::const_iterator it_j = m.find(bterEdges.phase_one_j[i]);
//
//        // If key does not exist insert new key - value
//        if (it_i == m->end() && it_j == m->end()) {
//            std::pair<int, vector<int>> edge(bterEdges.bter_phase_one_i[i], vector<int> node(bterEdges.bter_phase_two_j[i]));
//            m->insert(edge);
//        } else {
//            // Else insert only value in vector
//            m->at()
//        }
//    }
}